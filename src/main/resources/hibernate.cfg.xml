<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Pilote MySQL -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <!-- URL de connexion à la base daara -->
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/daara?serverTimezone=Africa/Dakar&amp;useSSL=false
        </property>

        <!-- Identifiants XAMPP par défaut -->
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- Dialecte MySQL -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
        </property>

        <!-- Crée les tables automatiquement si elles n'existent pas -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Affiche les requêtes SQL dans la console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

    </session-factory>
</hibernate-configuration>